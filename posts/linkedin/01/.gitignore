# Generated by ../make_package.py from README.md — never edit, never commit.
# LINKEDIN.md is a second copy of the article (a committed copy drifts from README.md the
# moment either is edited), and linkedin.zip is megabytes of binary that would re-enter
# history on every rebuild. Rebuild both with:
#   uv run python posts/linkedin/make_package.py
LINKEDIN.md
linkedin.zip
