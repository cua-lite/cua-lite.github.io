# Generated by ../make_package.py from README.md — never edit, never commit.
# TWITTER.md is a second copy of the posts (a committed copy drifts from README.md the
# moment either is edited), and twitter.zip is megabytes of binary that would re-enter
# history on every rebuild. Rebuild both with:
#   uv run python posts/twitter/make_package.py
TWITTER.md
twitter.zip
assets.zip
